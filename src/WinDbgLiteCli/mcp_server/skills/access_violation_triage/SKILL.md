# Access Violation Triage

Use this skill when `exceptionCode` is `0xC0000005`.

The workflow collects exception information, registers, stack, memory map, and nearby disassembly. It is read-only and should be safe to run automatically.
