# Exception Triage

Use this skill when the debuggee stops on an exception and you need a first-pass read-only snapshot.

The workflow collects the stop record, exception details, registers, stack, modules, and breakpoints. It does not resume the process or modify debugger state.
