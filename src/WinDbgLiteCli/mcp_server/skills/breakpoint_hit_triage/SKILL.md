# Breakpoint Hit Triage

Use this skill after a software, hardware, or memory breakpoint hits.

The workflow gathers stop info, breakpoints, registers, stack, and a short disassembly view. It is read-only and is safe to run automatically after a pause.
